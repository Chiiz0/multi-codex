# Task Envelope Reference

Main Codex must create a Task Envelope before any worker starts. The envelope must describe role, executor, skill, agent profile, allowed paths, forbidden paths, objective, acceptance criteria, stop conditions, and required outputs.
